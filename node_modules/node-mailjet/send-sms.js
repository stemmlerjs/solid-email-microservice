const Mailjet = require('./mailjet-client')

const config = {
  'version': 'v4'
}

const body = {
  From: 'MJPilot',
  To: '+33600000000',
  'Text': 'Have a nice SMS flight with Mailjet !'
}

const client = new Mailjet(process.env.MJ_API_TOKEN)

// client
//   .post('sms-send')
//   .request(body)
//   .then((e) => console.log(e.body))
//   .catch((e) => console.dir(e.ErrorMessage))


const body1 = {
  'FromTs' : 1033552800,
  'ToTS': 1033552800
}

client
  .post('sms')
  .action('export')
  .request(body1)
  .then((response) => console.log(response.body))
  .catch((error) => console.log('error: ' + JSON.stringify(error)))